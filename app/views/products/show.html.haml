.user-top--btn
  = link_to "TopPage", products_path, class: "user-top--btn-link"
.aa
  = @product.title
.products-top--image--left--objects
  = image_tag @product.image.to_s
.a
製作中
- if current_user.id == @product.user_id
  .sa さぃ

-else
  .aa hoge
